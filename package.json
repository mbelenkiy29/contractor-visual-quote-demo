{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test:redesigns": "vitest run artifacts/api-server/test/redesigns.test.ts",
    "test:redesigns:browser": "playwright install chromium && playwright test -c artifacts/contractor-visual-quote-demo/playwright.config.ts",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@clerk/express": "^2.1.69",
    "@clerk/react": "^6.16.1",
    "@clerk/shared": "^4.33.0",
    "@clerk/themes": "^2.4.57",
    "@replit/connectors-sdk": "^0.4.3",
    "http-proxy-middleware": "^4.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3",
    "vitest": "^5.0.1"
  }
}
